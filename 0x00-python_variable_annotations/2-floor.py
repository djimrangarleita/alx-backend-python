import math


def floor(n: float) -> int:
    """Floor the number passed as argument

    Args:
        n (float): The number to be floored

    Returns:
        int: The int returned after applying the floor function
    """
    return math.floor(n)